document.querySelectorAll('.littleblock').forEach((lilblok: HTMLElement) => {
    console.log('test')
    // lilblok.style.rotate = '1px'
    // lilblok.style.transform = `rotate(${2 * 2 * (-0.5 + Math.random())}px)`
})